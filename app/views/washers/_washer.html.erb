<tbody>
	<tr>
		<td><%= washer.channel %></td>
		<td><%= washer.start_at %></td>
		<td><%= washer.intervel %></td>
		<td><%= washer.start_stop %></td>
		<td><%= link_to "edit", washer, method: :edit%></td>
		<td><%= link_to "delete", washer, method: :delete, data: { confirm: "Are you sure?" } %></td>
	</tr>
</tbody>