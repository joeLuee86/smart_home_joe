<% provide(:title, "SmartWash") %>

<h1>Welcome to Smart Washing System</h1>

<div class="row">
	<div class="col-md-6 col-sm-6">
	  <%= image_tag "washing.jpg" %>
	</div>
	<div class="col-md-6">
		<h2>Schedule List</h2>
		<ul class="smart_wash">
		  <% @smart_wash_schdules.each do |schedule| %>
		    <%= render @schedules %>
		  <% end %>
		</ul>
  		<%= form_tag 'smart_wash' do |f| %>
  			<div><%= submit_tag "Create Schedule", class: "btn btn-primary" %></div>
  		<% end %>
	</div>
</div>

